---
title: Hello, world
date: 2026-07-18
---

This profile is a git repo. This post is a markdown file. The commit that
added it said `post: hello world`, which cut a release — so you can follow
me with any feed reader pointed at this repo's `releases.atom`.

Delete this file and write your own. The filename convention is
`YYYY-MM-DD-your-slug.md`, and the front matter block at the top is
optional — title falls back to the first heading or the filename, date
falls back to the filename prefix.
